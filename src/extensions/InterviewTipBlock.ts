import { createCalloutBlock } from './createCalloutBlock'

export const InterviewTipBlock = createCalloutBlock({
  variant: 'interview-tip',
  defaultTitle: 'Interview Tip',
  markdownTag: 'interview-tip',
})
